name: eduroam CAT (Android) master

on:
  push:
    branches: [ ci-test ]
  pull_request:
    branches: [ ci-test ]

jobs:
  build:

    runs-on: mreichelt/android:29

    steps:
    - name: Build with Gradle
      run: ./gradlew build
